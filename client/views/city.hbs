<html>
  <head>
    <title>{{title}}</title>
  </head>
  <body>
    <h1>{{title}}</h1>
    <ul>
      {{#each meetup.results}}
      <li><a href="{{event_url}}">{{name}}</a>
        <p>{{{description}}}</p>
      </li>
      {{/each}}
    </ul>
    <hr />
    <footer>
      <small>Rendered by Handlebars</small>
    <footer>
  </body>
<html>
